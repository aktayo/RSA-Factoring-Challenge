#!/usr/bin/python3
"""
            # calculate the private key d
            d = pow(e, -1, factor - 1)
            # print the results
            print('The factors of', n, 'are', factors)
            print('The private key d is', d)

# test the RSA factoring challenge program
RSA_factoring_challenge(97, 3)
